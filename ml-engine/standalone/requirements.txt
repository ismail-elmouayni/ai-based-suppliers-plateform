# Standalone pipeline dependencies
# Database and API server deps (pyodbc, sqlalchemy, flask) are intentionally excluded.
#
# Version constraints use >= so this installs cleanly across pip versions.
# The original codebase pins exact versions (requirements.txt at the workspace root)
# for reproducible DB deployments; this file prioritises ease of local installation.

# Core data
pandas>=2.0.0
numpy>=1.24.0

# ML
scikit-learn>=1.3.0
rapidfuzz>=3.0.0

# Excel I/O
openpyxl>=3.1.0        # read input xlsx + verify output in tests
xlsxwriter>=3.2.0      # write output xlsx with charts

# Config
pyyaml>=6.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
